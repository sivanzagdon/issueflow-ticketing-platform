/**
 * Restore flows are documented in README (POST .../restore) but not implemented yet.
 * Replace these todos with real specs when ProjectsService/TicketsService.restore exist.
 */
describe('Audit log restore coverage (pending implementation)', () => {
  describe('ProjectsService.restore', () => {
    it.todo(
      'pending: POST /projects/:projectId/restore records RESTORE audit with actor USER',
    );
  });

  describe('TicketsService.restore', () => {
    it.todo(
      'pending: POST /tickets/:ticketId/restore records RESTORE audit with actor USER',
    );
  });
});
